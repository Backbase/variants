#
# ------
#
# Auto generated by Variants
# TODO: Replace all values that contain {{ REPLACE_ME }}
#
# ------
#

ios:
    xcodeproj: {{ REPLACE_ME }}.xcodeproj
    targets:
      PeachTree:
        name: {{ REPLACE_ME }}
        bundle_id: {{ REPLACE_ME }}
        icon: AppIcon
        source:
          path: {{ REPLACE_ME }}
          info: {{ REPLACE_ME }}/Info.plist
    variants:
        # Default variant is mandatory, do not remove
      - name: default
        version_name: 0.0.1
        version_number: 1
        # custom:
        # You can have as many custom fields as possible.
        # Only strings allowed
        # These will be available to you via `Variants.customConfig` dictionary
        # i.e.: `Variants.customConfig["sample_property"]`
        custom:
            OTHER_SWIFT_FLAGS: $(inherited)
        # Sample variant, "beta".
        # Only name and bundle_id_suffix are mandatory fields
      - name: beta
        bundle_id_suffix: beta
        version_name: 0.0.1
        version_number: 1
