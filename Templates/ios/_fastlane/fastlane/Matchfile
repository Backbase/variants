
# git_url("git@github.com:sample/match.git")
storage_mode("git")

# appstore, development, adhoc, enterprise
type("appstore")

app_identifier([
    "com.backbase.bundle_id",
    "com.backbase.bundle_id.beta"
])
