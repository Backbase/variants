#
# ------
#
# Auto generated by MobileSetup
# TODO: Replace all values that contain REPLACE
#
# ------
# This YAML file is used by both mobile-setup and xcodegen
#
name: "{REPLACE_AppName}"
options:
  deploymentTarget:
    iOS: 12.0
targets:
  myApp:
    type: application
    platform: iOS
    deploymentTarget:
      iOS: 12.0
    settings:
      base:
        MARKETING_VERSION: 0.0.1
    info:
      path: "{REPLACE_Source/Info.plist}"
      properties:
        CFBundleShortVersionString: $(MARKETING_VERSION)
        CFBundleName: $(APP_NAME)
        CFBundleVersion: $(BUILD_NUMBER)
        CFBundleIdentifier: $(PRODUCT_BUNDLE_IDENTIFIER)
        BB_CONFIG_PATH: $(BB_CONFIG_PATH)
        UILaunchStoryboardName: LaunchScreen
        UIRequiredDeviceCapabilities: armv7
        UISupportedInterfaceOrientations:
          - UIInterfaceOrientationPortrait
        NSFaceIDUsageDescription: "Allow FaceID as a form of passwordless authentication"
        NSAppTransportSecurity:
          NSAllowsArbitraryLoads: false
          NSExceptionDomains:
            "{REPLACE backbase.test}":
              NSIncludesSubdomains: true
              NSTemporaryExceptionAllowsInsecureHTTPLoads: true
              NSTemporaryExceptionMinimumTLSVersion": TLSv1.1
    sources: 
      - path: "{REPLACE_Source}"
        excludes: Info.plist
        createIntermediateGroups: true
    scheme:
      gatherCoverageData: true
# MobileSetup uses the configuration below
setupConfiguration:
  configPath: "{REPLACE Resources/assets/backbase/conf}"
  baseBundleId: "{REPLACE com.peachtree.mobile"
  environments: 
    - env: "dev"
      cxp: "https://dev.env"
      identity: "https://identity.dev"
    - env: "qa"
      cxp: "https://qa.env"
      identity: "https://identity.qa"

