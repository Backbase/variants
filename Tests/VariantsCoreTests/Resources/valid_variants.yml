#
# Auto generated by Variants
#

android:
  path: path/to/android-project/
  app_name: FrankBank
  app_identifier: com.backbase.frank
  variants:
    - name: default
      version_name: 0.0.1
      version_code: 1
      task_build: bundleProdRelease
      task_unittest: testProdReleaseUnitTest
      task_uitest: connectedProdReleaseAndroidTest
      store_destination: PlayStore
      custom:
        - name: SAMPLE_ENV
          value: Sample Environment Config
          destination: environment
    - name: test
      id_suffix: dev
      version_name: 0.0.1
      version_code: 1
      task_build: assembleDevelop
      task_unittest: testDevDebugUnitTest
      task_uitest: connectedDevDebugAndroidTest
      store_destination: AppCenter
      custom:
        - name: SAMPLE_FASTLANE
          value: Sample Fastlane Config
          destination: fastlane
        - name: SAMPLE_PROJECT
          value: Sample Project Config
          destination: project
  
  # ----------------------------------------------------------------------
  # signing: - Not required.
  #
  # When uncommented, the information below will be available in the file
  # `variants.gradle`, found in `{{ android.path }}/gradleScripts/` folder
  #
  # Uncomment section below if necessary.
  # ----------------------------------------------------------------------

  signing:
      key_alias: android
      key_password: envVars.APP_SIGN_KEY_PASSWORD
      store_file: /usr/local/android/production.keystore
      store_password: envVars.APP_SIGN_STORE_PASSWORD
  
  
  # ----------------------------------------------------------------------
  # custom: - Not required.
  #
  # Same as variant's `custom`, but this will be processed regardless of
  # the chosen variant.
  #
  # Uncomment section below if necessary.
  # ----------------------------------------------------------------------

  custom:
    - name: SAMPLE_GLOBAL
      value: GLOBAL Value Android
      destination: project

ios:
    xcodeproj: FrankBank.xcodeproj
    targets:
      FrankBank:
        name: FrankBank
        bundle_id: com.backbase.frank.ios
        app_icon: AppIcon
        source:
          path: Sources
          info: Sources/Info.plist
          config: Sources
    variants:
      - name: default
        version_name: 0.0.1
        version_number: 1
        custom:
            - name: SAMPLE_CONFIG
              value: Production Value
              destination: project
      - name: BETA
        id_suffix: beta
        app_icon: AppIcon.beta
        version_name: 0.0.1
        version_number: 1
        store_destination: AppCenter
        custom:
            - name: SAMPLE_CONFIG
              value: BETA Value
              destination: fastlane
    custom:
        - name: SAMPLE_GLOBAL
          value: GLOBAL Value iOS
          destination: project
